# Local dev broker. Two listeners: native MQTT + WebSocket (for web app browser bridge if used directly).
# Allow anonymous for local dev only. Production: provision creds + ACLs in FlowFuse.

persistence true
persistence_location /mosquitto/data/
log_dest stdout

listener 1883
protocol mqtt
allow_anonymous true

listener 9001
protocol websockets
allow_anonymous true
