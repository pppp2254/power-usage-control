# Solar-Aware Load Controller — Foundation

The canonical foundation/spec lives in [`CLAUDE.md`](./CLAUDE.md) (kept there so
Claude Code reads it automatically). This file previously held a byte-identical
copy, which risked the two diverging — edit `CLAUDE.md` only.

Contracts (API + MQTT) are mirrored in [`docs/contracts.md`](./docs/contracts.md).
